<div class="notifications">
  <% @notifications.each do |notification| %>
    <div class="panel panel-default <%= notification.unread? ? 'unread' : 'read' %>">
      <%= render_notification_partial(notification) %>
    </div>
  <% end %>
</div>