<h2>Posts for #<%= @hashtag %> (<%= @posts_count %> total)</h2>

<% @posts.each do |post| %>
  <%= render 'post', post: post %>
<% end %>

<% if @posts.empty? %>
  <p>No posts for #<%= @hashtag %></p>
<% end %>